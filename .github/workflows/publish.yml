name: Publish

on:
  push:
    tags:
      - v*

permissions:
  contents: write

jobs:
  publish:
    uses: h13/.github/.github/workflows/publish-node.yml@e8b11c84ef0912857e537f6284e4ea4522abb420 # main
    secrets: inherit
