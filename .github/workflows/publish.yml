name: Publish

on:
  push:
    tags:
      - v*

permissions:
  contents: write

jobs:
  publish:
    uses: h13/.github/.github/workflows/publish-node.yml@f6fcf9f19ad292ead08ea26926a178612cd7ba60 # main
    secrets: inherit
