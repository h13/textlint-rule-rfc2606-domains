name: Publish

on:
  push:
    tags:
      - v*

permissions:
  contents: write

jobs:
  publish:
    uses: h13/.github/.github/workflows/publish-node.yml@f5c87c25ad2b0dff3c6a26dc3fc2b47963490e43 # main
    secrets: inherit
