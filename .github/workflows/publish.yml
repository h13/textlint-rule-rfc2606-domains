name: Publish

on:
  push:
    tags:
      - v*

permissions:
  contents: write

jobs:
  publish:
    uses: h13/.github/.github/workflows/publish-node.yml@a4844a3b43db07bbad3f79502f5f26c2c8ce8028 # main
    secrets: inherit
