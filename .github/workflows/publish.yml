name: Publish

on:
  push:
    tags:
      - v*

permissions:
  contents: write

jobs:
  publish:
    uses: h13/.github/.github/workflows/publish-node.yml@2c5a5cc19fecc85692a26d290acb9e55611fb7df # main
    secrets: inherit
