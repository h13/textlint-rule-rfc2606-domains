name: Publish

on:
  push:
    tags:
      - v*

permissions:
  contents: write

jobs:
  publish:
    uses: h13/.github/.github/workflows/publish-node.yml@18b85640f205a62c7db36d068f1fe68be91fec85 # main
    secrets: inherit
