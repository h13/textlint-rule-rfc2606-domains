name: Publish

on:
  push:
    tags:
      - v*

permissions:
  contents: write

jobs:
  publish:
    uses: h13/.github/.github/workflows/publish-node.yml@5d5f479eada75789f23d231360b0985c4847dc83 # main
    secrets: inherit
