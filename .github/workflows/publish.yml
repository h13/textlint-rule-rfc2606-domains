name: Publish

on:
  push:
    tags:
      - v*

permissions:
  contents: write

jobs:
  publish:
    uses: h13/.github/.github/workflows/publish-node.yml@7ac717af3d6161ae98f60b114c8bed35f82312d6 # main
    secrets: inherit
