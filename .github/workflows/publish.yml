name: Publish

on:
  push:
    tags:
      - v*

permissions:
  contents: write

jobs:
  publish:
    uses: h13/.github/.github/workflows/publish-node.yml@a59f0dabdc743cb62a184d9da60db89c0a3f42c9 # main
    secrets: inherit
