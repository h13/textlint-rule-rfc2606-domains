name: Publish

on:
  push:
    tags:
      - v*

permissions:
  contents: write

jobs:
  publish:
    uses: h13/.github/.github/workflows/publish-node.yml@87f9aa633c43012011da9175ff11d7cfd0cd6096 # main
    secrets: inherit
