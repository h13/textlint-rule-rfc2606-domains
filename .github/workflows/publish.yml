name: Publish

on:
  push:
    tags:
      - v*

permissions:
  contents: write

jobs:
  publish:
    uses: h13/.github/.github/workflows/publish-node.yml@61231cf17d4f458c97772283fd8ba194acaf15cf # main
    secrets: inherit
