name: Publish

on:
  push:
    tags:
      - v*

permissions:
  contents: write

jobs:
  publish:
    uses: h13/.github/.github/workflows/publish-node.yml@86a82eaa38b6ffd40d1085107e2114c3cb0174af # main
    secrets: inherit
