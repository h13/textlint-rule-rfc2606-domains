name: Publish

on:
  push:
    tags:
      - v*

permissions:
  contents: write

jobs:
  publish:
    uses: h13/.github/.github/workflows/publish-node.yml@ae0479f602b0926169cd190639618edf10ad4dea # main
    secrets: inherit
