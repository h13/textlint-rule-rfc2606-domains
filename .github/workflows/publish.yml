name: Publish

on:
  push:
    tags:
      - v*

permissions:
  contents: write

jobs:
  publish:
    uses: h13/.github/.github/workflows/publish-node.yml@0885541c29dc75cf6e56d3767759bb263c75cc70 # main
    secrets: inherit
