name: Publish

on:
  push:
    tags:
      - v*

permissions:
  contents: write

jobs:
  publish:
    uses: h13/.github/.github/workflows/publish-node.yml@98addba86325a0c456e2bbbbbde9b50fec843c26 # main
    secrets: inherit
