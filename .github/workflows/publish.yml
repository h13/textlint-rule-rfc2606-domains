name: Publish

on:
  push:
    tags:
      - v*

permissions:
  contents: write

jobs:
  publish:
    uses: h13/.github/.github/workflows/publish-node.yml@fd87f1d4a5f3134890ba700f08b060a3292626c7 # main
    secrets: inherit
