name: Publish

on:
  push:
    tags:
      - v*

permissions:
  contents: write

jobs:
  publish:
    uses: h13/.github/.github/workflows/publish-node.yml@9b65556e189fd81570a7e400d121c068a2554ab3 # main
    secrets: inherit
