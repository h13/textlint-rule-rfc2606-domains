name: Publish

on:
  push:
    tags:
      - v*

permissions:
  contents: write

jobs:
  publish:
    uses: h13/.github/.github/workflows/publish-node.yml@38a9556d51590a0ddfebedae7bd6c1c66763fef6 # main
    secrets: inherit
