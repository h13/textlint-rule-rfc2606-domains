name: Publish

on:
  push:
    tags:
      - v*

permissions:
  contents: write

jobs:
  publish:
    uses: h13/.github/.github/workflows/publish-node.yml@0ba96d7f038232bf5969fe0f4dab9e0152fbdf6c # main
    secrets: inherit
