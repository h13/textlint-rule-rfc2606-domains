name: Publish

on:
  push:
    tags:
      - v*

permissions:
  contents: write

jobs:
  publish:
    uses: h13/.github/.github/workflows/publish-node.yml@f42496f424d8f73772590cec3ef7e7935f7f91d5 # main
    secrets: inherit
