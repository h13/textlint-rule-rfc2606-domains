name: Publish

on:
  push:
    tags:
      - v*

permissions:
  contents: write

jobs:
  publish:
    uses: h13/.github/.github/workflows/publish-node.yml@0838556b726843fcc8cdc655f6f7323227d424f5 # main
    secrets: inherit
