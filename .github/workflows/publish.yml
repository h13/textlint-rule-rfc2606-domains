name: Publish

on:
  push:
    tags:
      - v*

permissions:
  contents: write

jobs:
  publish:
    uses: h13/.github/.github/workflows/publish-node.yml@92b6e9a847dca134fed644e77a320e39f6cce62c # main
    secrets: inherit
